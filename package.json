{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc-alias -p tsconfig.json",
    "postbuild": "mkdir -p dist/src/infrastructure/repositories && cp src/infrastructure/repositories/PeriodicTable.json dist/src/infrastructure/repositories/PeriodicTable.json",
    "start": "node dist/main.js",
    "seed:periodic-table": "node dist/infrastructure/mongoose/seedPeriodicTable.js",
    "test": "vitest --run"
  },
  "devDependencies": {
    "@types/mongoose": "^5.11.96",
    "@types/node": "^25.2.3",
    "tsc-alias": "^1.8.16",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "mongoose": "^9.2.3"
  }
}
